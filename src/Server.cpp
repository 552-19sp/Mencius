#include <iostream>

int main() {
    std::cout << "Server" << std::endl;


    /*
        - Accept list of servers from command line.
        - Ping every server (heartbeat)
        - Implement KV Store
            * At most once semantics
        - Implement PMMC (assume no failures)
            * Async
        - Handle failure case
        - Set up integration tests
    */
}
